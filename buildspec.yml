version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing source NPM dependencies...
      - npm i -g yarn typescript @nestjs/cli

  build:
    commands:
      - yarn
      - yarn add sharp --ignore-engines
      # - yarn generate:sql && yarn generate:mongo
      - yarn build
  # post_build:
  #   commands:
  #     -
artifacts:
  files:
    - '**/*'
